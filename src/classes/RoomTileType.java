package classes;

/**
 * Represents the different types of tiles in a room. Each type defines the characteristics
 * of the tile.
 */
public enum RoomTileType {
    EMPTY,
    WALL,
    PLAYER,
    ENEMY,
    POWERUP
}